{
  "name": "text-search-tauri",
  "version": "1.0.0",
  "description": "Text Search desktop client (Tauri)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "node scripts/build-sidecar.mjs && tauri dev",
    "tauri:build": "node scripts/build-sidecar.mjs --release && tauri build"
  },
  "dependencies": {
    "@arco-design/web-vue": "^2.57.0",
    "@tauri-apps/api": "^2.4.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.4.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.4.0"
  }
}
