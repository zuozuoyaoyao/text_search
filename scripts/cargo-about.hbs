# Rust dependencies

This section is generated by `cargo-about` from the locked Cargo dependency
graph. The full license text is included below each SPDX license.

{{#each licenses}}
## {{{name}}} (`{{{id}}}`)

Used by:
{{#each used_by}}
- {{{crate.name}}} {{{crate.version}}}
{{/each}}

```text
{{{text}}}
```

{{/each}}
